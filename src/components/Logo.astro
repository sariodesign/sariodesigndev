<div class="logo">
	<a class="logo__link" cursor-class="link" href="/" data-astro-reload>{'<sario/design>'}</a>
</div>

<style lang="scss">
	.logo {
		font-size: 18px;
		font-weight: 400;
		letter-spacing: 2px;
		position: relative;
		z-index: 1;

		&__link {
			text-decoration: none;
			@media (prefers-color-scheme: dark) {
				color: var(--white);
			}
			@media (prefers-color-scheme: light) {
					color: var(--dark-black);
			}
		}

	}
</style>